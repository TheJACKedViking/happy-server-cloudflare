# Happy Server - Staging Environment
#
# This file is a template for staging environment.
# Copy to .env and fill in your actual staging values.
#
# SECURITY: Never commit this file with real credentials.

# ============================================
# REQUIRED - Database
# ============================================

# PostgreSQL connection string (staging database)
DATABASE_URL=postgresql://user:password@staging-db.example.com:5432/happy_staging

# Redis connection URL for pub/sub and caching
REDIS_URL=redis://staging-redis.example.com:6379

# ============================================
# REQUIRED - S3 Storage
# ============================================

# S3-compatible storage (use Cloudflare R2 or AWS S3)
S3_HOST=your-staging-s3-host.example.com
S3_ACCESS_KEY=your-staging-access-key
S3_SECRET_KEY=your-staging-secret-key
S3_BUCKET=happy-staging
S3_PUBLIC_URL=https://happy-staging.example.com
S3_USE_SSL=true

# ============================================
# REQUIRED - Security
# ============================================

# Master secret for authentication and encryption
# Generate with: openssl rand -hex 32
# IMPORTANT: Use a DIFFERENT secret than production!
HANDY_MASTER_SECRET=your-staging-master-secret

# ============================================
# Server Configuration
# ============================================

# HTTP server port
PORT=3005

# Node environment
NODE_ENV=staging

# ============================================
# OPTIONAL - Monitoring
# ============================================

# Enable Prometheus metrics
METRICS_ENABLED=true
METRICS_PORT=9090

# ============================================
# OPTIONAL - Voice Integration (ElevenLabs)
# ============================================

# ElevenLabs API key for voice synthesis (staging key recommended)
# ELEVENLABS_API_KEY=your-staging-elevenlabs-key

# ============================================
# DEBUG - Use with caution
# ============================================

# Enable debug logging endpoint (may be useful for staging debugging)
# DANGEROUSLY_LOG_TO_SERVER_FOR_AI_AUTO_DEBUGGING=true
